#include <stdio.h>

int main(void) {
    // const int INPUT_VALUES = 10;
    // int nilai[INPUT_VALUES];
    // int i, max, min, sum = 0;
    // float average;

    // for (i = 0; i < INPUT_VALUES; ++i) {
    //     scanf("%d", &nilai[i]);
    // }

    // max = nilai[0];
    // min = nilai[0];

    // for (i = 0; i < INPUT_VALUES; ++i) {
    //     if (nilai[i] > max) {
    //         max = nilai[i];
    //     }
    //     if (nilai[i] < min) {
    //         min = nilai[i];
    //     }
    //     sum += nilai[i];
    // }

    // average = (float)sum / INPUT_VALUES;

    // printf("Nilai maksimum: %d\n", max);
    // printf("Nilai minimum: %d\n", min);
    // printf("Rata-rata: %.2f\n", average);


    return 0;
}
