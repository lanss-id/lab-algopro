#include <stdio.h>

int main() {
    // int A = 10;
    // int B = 5;
    // char* C;

    // // Operasi pada A dan B
    // A += 2;  // A = A + 2, sekarang A = 12
    // A /= 2;  // A = A / 2, sekarang A = 6
    // A -= B;  // A = A - B, sekarang A = 1
    // B ^= A + 1;  // B = B ^ (A + 1), sekarang B = 7

    // C = (B % 5 == 0) ? "multiple" : "not multiple";

    // printf("Nilai A: %d\n", A);
    // printf("Nilai B: %d\n", B);
    // printf("C adalah: %s\n", C);

    char s[100];
    int a;

    scanf("%s", s);
    scanf("%d", &a);

    printf("%s", s);
    printf("%d", a);

    return 0;
}
